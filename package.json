{
  "version": "5.0.0",
  "scripts": {
    "test": "composer run test",
    "release": "bumpp",
    "test:format": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.6",
    "bumpp": "^9.1.1",
    "prettier": "^2.0"
  }
}
