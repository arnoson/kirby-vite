{
  "version": "4.0.3",
  "scripts": {
    "test": "composer run test",
    "release": "bumpp -r",
    "test:format": "prettier . --check"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.6",
    "bumpp": "^9.1.1",
    "prettier": "^2.0"
  }
}
