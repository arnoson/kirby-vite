{
  "version": "5.2.0",
  "scripts": {
    "test": "composer run test",
    "release": "bumpp",
    "test:format": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.22.2",
    "bumpp": "^9.1.1",
    "prettier": "^3.0"
  }
}
