{
  "version": "5.0.1",
  "scripts": {
    "test": "composer run test",
    "release": "bumpp",
    "test:format": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.21.0",
    "bumpp": "^9.1.1",
    "prettier": "^3.0"
  }
}
