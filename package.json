{
  "version": "3.1.0",
  "scripts": {
    "test": "composer run test",
    "release": "bumpp --no-push -r"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.6",
    "bumpp": "^9.1.1",
    "prettier": "^2.0"
  }
}
